// Package version holds the release version string used by CI and the CLI.
// The value must match the pushed git tag: tag v${Version} (e.g. Version "1.2.3" → tag v1.2.3).
package version

const Version = "0.0.1"
