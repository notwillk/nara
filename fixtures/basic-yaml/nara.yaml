version: 1
paths:
  "/": entries
meta:
  ref: "$ref"
  id: "$id"
  schema: "$schema"
  includeKeys: []
schemas:
  sources:
    - schemas/*.cue
  inferFromFilename: true
resolution:
  filenamePattern: <id>.<schema>
  extensions:
    - .yaml
    - .yml
    - .json
